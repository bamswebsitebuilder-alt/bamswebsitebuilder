BAM'S WEBSITE BUILDER - HOME MATCHED FIX

This package standardizes the uploaded English HTML files around the root homepage.

Fixed:
- index.html and home.html are now identical, so the two homepage files cannot display different code.
- All Home, logo, desktop, mobile, and language links on home.html use /.
- The homepage canonical URL uses https://bamswebsitebuilder.com/.
- index.html and home.html both load /css/page-theme.css?v=1 like the other public pages.
- Blush Bloom remains on index.html, home.html, and portfolio.html.
- Blush Bloom buttons point to https://www.bamswebsitebuilder.com/blush-bloom/.
- Uploaded numbered filenames were renamed to clean production filenames.
- Page-specific portal/admin CSS and JavaScript were preserved.

Important:
Keep your existing css, js, images, es, and project-demo folders in the same website root when uploading these files.
