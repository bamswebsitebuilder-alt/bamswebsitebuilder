const menuButton=document.querySelector('.menu-button');
menuButton?.addEventListener('click',()=>{document.body.classList.toggle('mobile-open');menuButton.setAttribute('aria-expanded',document.body.classList.contains('mobile-open'))});
document.querySelectorAll('[data-track-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const input=form.querySelector('input');const result=form.querySelector('.result');const code=input.value.trim();if(!code){result.textContent='Enter a FleetFlow reference number to view the demo timeline.'}else{result.innerHTML=`<strong>${code.toUpperCase()}</strong><br>In transit · Route optimized · Estimated arrival tomorrow at 2:30 PM`;document.querySelector('[data-demo-timeline]')?.removeAttribute('hidden')}result.style.display='block'}));
document.querySelectorAll('[data-consult-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const result=form.querySelector('.result');result.textContent='Demo request received. A production website could send this information to a CRM or sales team.';result.style.display='block'}));
