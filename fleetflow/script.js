document.querySelector('.menu-button')?.addEventListener('click',()=>document.body.classList.toggle('mobile-open'));
document.querySelectorAll('[data-track-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const input=form.querySelector('input');const result=form.querySelector('.result')||document.querySelector('#tracking-result');const code=input.value.trim();if(!code){result.textContent='Please enter a tracking number.'}else{result.innerHTML='<strong>'+code.toUpperCase()+'</strong><br>Demo status: In transit — scheduled delivery in 2 business days.'}result.style.display='block'}));
document.querySelectorAll('[data-quote-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const result=form.querySelector('.result')||document.querySelector('#quote-result');result.textContent='Demo quote created. Estimated shipping range: $86–$142. A real website could connect this form to a carrier API.';result.style.display='block'}));
